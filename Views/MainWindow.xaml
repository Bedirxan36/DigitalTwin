<Window x:Class="DigitalTwin.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Digital Twin" 
        Height="700" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundPrimaryBrush}">
    
    <Window.Resources>
        <!-- Page Transition Animation -->
        <Storyboard x:Key="PageFadeIn">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                           From="0" To="1" Duration="0:0:0.4">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <ThicknessAnimation Storyboard.TargetProperty="Margin"
                              From="0,20,0,0" To="0,0,0,0" Duration="0:0:0.4">
                <ThicknessAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </ThicknessAnimation.EasingFunction>
            </ThicknessAnimation>
        </Storyboard>
    </Window.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar Navigation -->
        <Border Grid.Column="0" Background="{StaticResource SidebarBrush}" Padding="16">
            <StackPanel>
                <TextBlock FontSize="24" 
                          FontWeight="Bold" 
                          Foreground="{StaticResource TextOnDarkBrush}" 
                          Margin="8,16,0,40">
                    <Run Text="Digital" Foreground="{StaticResource PrimaryLightBrush}"/>
                    <Run Text=" Twin"/>
                </TextBlock>
                
                <Button Content="📊 Kontrol Paneli" 
                       Click="NavigateToDashboard"
                       Style="{StaticResource NavButtonStyle}"
                       Margin="0,4"/>
                
                <Button Content="💡 İçgörüler" 
                       Click="NavigateToInsights"
                       Style="{StaticResource NavButtonStyle}"
                       Margin="0,4"/>
                
                <Button Content="🎯 Odak Oturumu" 
                       Click="NavigateToFocusSession"
                       Style="{StaticResource NavButtonStyle}"
                       Margin="0,4"/>
                
                <Button Content="🏷️ Kurallar ve Etiketler" 
                       Click="NavigateToRules"
                       Style="{StaticResource NavButtonStyle}"
                       Margin="0,4"/>
                
                <Button Content="⚙️ Ayarlar" 
                       Click="NavigateToSettings"
                       Style="{StaticResource NavButtonStyle}"
                       Margin="0,4"/>
                
                <Button Content="ℹ️ Hakkında" 
                       Click="NavigateToAbout"
                       Style="{StaticResource NavButtonStyle}"
                       Margin="0,4"/>
            </StackPanel>
        </Border>

        <!-- Content Area with Animation -->
        <Border Grid.Column="1" 
                Margin="24"
                Background="Transparent">
            <Frame x:Name="ContentFrame" 
                   NavigationUIVisibility="Hidden"
                   Background="Transparent"/>
        </Border>
    </Grid>
</Window>
